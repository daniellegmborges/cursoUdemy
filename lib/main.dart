import 'package:flutter/material.dart';
import 'package:test1/src/desafio_button.dart'; //para rodar no app, alterar para qual aula deve ser lida

void main() {
  runApp(
    const AppAula74(), //para rodar no app, alterar para qual aula deve ser lida
  );
}
