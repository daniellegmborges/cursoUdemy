import 'package:flutter/material.dart';
import 'package:test1/src/app_aula65.dart'; //para rodar no app, alterar para qual aula deve ser lida

void main () {
  runApp(
    const AppAula65(), //para rodar no app, alterar para qual aula deve ser lida
  );
}
